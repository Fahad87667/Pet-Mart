{
  "name": "pet-mart",
  "version": "1.0.0",
  "description": "Pet Mart is a full stack application that allows users to browse and adopt pets.",
  "scripts": {
    "backend": "mvnw spring-boot:run",
    "frontend": "cd frontend && npm start",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "install-all": "cd frontend && npm install && cd .. && mvnw clean install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "npm-run-all": "^4.1.5"
  }
}
