{
  "name": "pet-mart",
  "version": "1.0.0",
  "description": "Pet Mart is a full stack application that allows users to browse and adopt pets.",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "mvnw.cmd spring-boot:run",
    "frontend": "cd frontend && npm run start",
    "install-all": "cd frontend && npm install && cd .. && mvnw.cmd clean install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
