import React from \'react\';\nimport { Card, Button } from \'react-bootstrap\';\nimport \'./FindPet.css\'; // We will create this CSS file next\n\nfunction ProductCard({ product }) {\n  const handleAddToCart = () => {\n    // Add to cart logic here\n    console.log(\`Add to cart clicked for product: ${product.code}\`);\n    // You would typically dispatch an action or call an API here\n  };\n\n  const handleEditProduct = () => {\n    // Edit product logic for admin\n    console.log(\`Edit clicked for product: ${product.code}\`);\n    // Redirect to admin edit page\n  };\n\n  const handleDeleteProduct = () => {\n    // Delete product logic for admin\n    console.log(\`Delete clicked for product: ${product.code}\`);\n    // Call delete API\n  };\n\n  // Placeholder for admin role check - replace with actual auth context\n  const isAdmin = true; \n\n  return (\n    <Card className=\"product-card h-100\">\n      <Card.Img variant=\"top\" src={product.imageUrl} alt={product.name} className=\"product-card-image\" />\n      <Card.Body className=\"d-flex flex-column\">\n        <Card.Title>{product.name}</Card.Title>\n        <Card.Text>\n          <strong>Code:</strong> {product.code}<br/>\n          <strong>Type:</strong> {product.type}<br/>\n          <strong>Price:</strong> ${product.price.toFixed(2)}\n        </Card.Text>\n        <div className=\"mt-auto\">\n           {/* Placeholder for user role check - replace with actual auth context */}\n          {!isAdmin ? (\n             <Button variant=\"primary\" onClick={handleAddToCart} className=\"w-100\">Add to Cart</Button>\n          ) : (\n            <div className=\"d-flex justify-content-between\">\n              <Button variant=\"warning\" onClick={handleEditProduct} className=\"w-50 me-1\">Edit</Button>\n              <Button variant=\"danger\" onClick={handleDeleteProduct} className=\"w-50 ms-1\">Delete</Button>\n            </div>\n          )}\n        </div>\n      </Card.Body>\n    </Card>\n  );\n}\n\nexport default ProductCard; 